# OBEY Start Here

**Purpose:** one-page orientation so Frank does not need to re-read the whole repo before every session.

---

## 1. What this project is

OBEY is the public product.

Its wedge is:

**make AI obey inside real workflows.**

Not in the abstract.
Not as a vague prompt pack.
Not as a fake autonomous employee.

The first concrete wedge is:

**command -> context -> approval -> execution**

The strongest current demo direction is:

**WhatsApp command -> asset generation -> approval -> Instagram publishing**

---

## 2. What each thing is

### OBEY

Public product, OSS wedge, future runtime.

### COS

Internal proactive operating layer.

It helps keep work moving.
It does not lead the company.
It does not replace founder judgment.

### SGOPS

Lab, GEO/SEO surface, documentation and public thinking.

It documents the category and the lessons.

---

## 3. What Frank should do

Frank is the CEO.

Frank should:

- choose direction
- approve tradeoffs
- bring real pain
- decide what is worth publishing

Frank should not:

- re-derive the architecture every day
- ask COS to act like a boss
- re-read the entire repo before every work session

---

## 4. What Codex should do

Codex is the architecture and implementation partner.

Codex should:

- keep the architecture coherent
- translate the current objective into implementation work
- choose the next build step
- update the repo state
- keep momentum

This means Frank does not need to open a session already knowing the entire roadmap.

---

## 5. Current project priority

The current priority is:

**build the first real OBEY wedge, not the whole empire.**

That means:

- do not start with a huge site-only push
- do not start with a generalized COS rewrite
- do not start with a fake full-agency promise

Instead:

- define the first real loop
- build the first real loop
- prove the first real loop

---

## 6. How sessions should work

Frank can start a session with something as simple as:

- "continue the next OBEY step"
- "implement the next piece of the wedge"
- "let's work on the publish flow"
- "what is the next thing to build?"

Codex should then:

- read the local OBEY state
- identify the current milestone
- implement the next bounded step
- explain what changed

Frank does **not** need to say:

"read everything and figure it out"

This file exists so that future sessions can anchor from here.

---

## 7. If opening a new chat

Use this instruction:

`Read /Users/fabianofrank/Desktop/Projetos/OBEY/START_HERE.md and continue the next implementation step for OBEY.`

That should be enough context to begin.

If the chat is for COS/Claude Desktop specifically, use:

`Read /Users/fabianofrank/Desktop/Projetos/OBEY/COS_BOOTSTRAP_PROMPT.md and adopt that operating model before doing anything else.`

---

## 8. Current recommended next move

Do **not** build the full public site first.

Do **not** rebuild COS as a huge abstract system first.

Build the minimum executable wedge:

1. OBEY project skeleton
2. command schema
3. project context loader
4. approval flow
5. publish adapter interface
6. demo-ready local workflow

Once that exists, the site becomes credible because it describes something real.
